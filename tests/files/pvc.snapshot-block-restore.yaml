apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lvm-pvc-snapshot-block-restore
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Block
  storageClassName: csi-driver-lvm-linear
  resources:
    requests:
      storage: 200Mi
  dataSource:
    apiGroup: snapshot.storage.k8s.io
    kind: VolumeSnapshot
    name: lvm-snapshot-block
